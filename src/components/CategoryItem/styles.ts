import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#59c493',
        paddingVertical: 12,
        marginBottom: 30,
        marginRight: 10,
        borderRadius: 20,
        minWidth: 100,
        height: 40,
        alignItems: 'center',
    },
    text: {
        color: '#fafafa',
        fontSize: 11,
    }
})
