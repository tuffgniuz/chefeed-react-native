import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
    },
    displayBox: {
        marginVertical: 20,
        marginHorizontal: 15,
        alignItems: 'center'
    },
    text: {
        fontSize: 14,
    }
})
