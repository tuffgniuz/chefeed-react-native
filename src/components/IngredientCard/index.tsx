import { FC } from "react"
import { View } from "react-native"

interface Props {
    name: string,
    icon: string,
}

const IngredientCard: FC<Props> = ({name, icon}) => {
    return (
        <View>
        </View>
    )
}
