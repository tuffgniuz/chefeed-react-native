import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    text: {
        marginVertical: 15,
        alignSelf: 'center',
        fontFamily: 'mansalva',
        color: '#59c49c',
        fontSize: 50,
    }
})
