import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        // marginTop: 30,
        alignSelf: 'center',
        alignItems: 'center',
    },
    userImage: {
        width: 150,
        height: 150,
        borderRadius: 100,
        borderWidth: 1,
        borderColor: '#282828',
        marginBottom: 25,
    },
    username: {
        fontSize: 30,
    }
})
