import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        // justifyContent: 'space-evenly',
        // width: '100%',
        marginBottom: 5,
    },
    img: {
        alignSelf: 'auto',
        width: 120,
        height: 120,
        borderRadius: 10,
    }
})
